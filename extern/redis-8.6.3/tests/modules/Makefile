# Minimal Makefile for tests/modules.
# The test modules are not needed for codis-server runtime builds.
# This file exists so the "module_tests" dependency of "all" does not fail.

.PHONY: all clean

all:

clean:
